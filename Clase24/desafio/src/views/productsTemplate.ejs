<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Desafio Websockets | Clase 12 | Coderhouse</title>
</head>
<body class="bg-dark">

    <h2 class="text-uppercase mb-4">Tabla de productos en tiempo real</h2>
    <% if (!products.length) {%>
        <h4>No hay productos para mostrar</h4>
    <% } else { %>  
        <table class="table table-dark table-hover align-middle">
            <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Nombre</th>
                <th scope="col">Precio</th>
                <th scope="col">Foto</th>
            </tr>
            </thead>
            <tbody>
                <% products.forEach(prod => { %>
                    <tr>
                        <th scope="row"><%= prod.id %></th>
                        <td><%= prod.title %></td>
                        <td><%= prod.price %></td>
                        <td><img class="img-fluid" src="<%= prod.thumbnail %>" alt="<%= prod.title %>" width="75rem"></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %> 

</body>